import { RenderTruePipe } from './render-true.pipe';

describe('RenderTruePipe', () => {
  it('create an instance', () => {
    const pipe = new RenderTruePipe();
    expect(pipe).toBeTruthy();
  });
});
